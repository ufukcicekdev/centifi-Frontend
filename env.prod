# Railway (web build) — EXPO_PUBLIC_* bundle’a build sırasında gömülür.
# Bu satırları Railway → frontend servisi → Variables’a ekleyin (veya .env üretim kopyası).
EXPO_PUBLIC_API_BASE_URL=https://centifi-backend-production.up.railway.app
