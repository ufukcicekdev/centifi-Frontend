/** iOS stub — bank automation UI is Android-only; keeps Play URLs out of the App Store binary. */
export function extractPlayStorePackageId(_input: string): string | null {
  return null;
}

export function playStoreDetailsUrl(_packageName: string): string {
  return "";
}
